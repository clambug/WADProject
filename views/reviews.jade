extends layout
block content
    .row
      .holder
        h1
          | Reviews
          .grid-container
            each review in reviews
             .item
              h1.itemtitle #{review.name}
              p.item #{review.content}